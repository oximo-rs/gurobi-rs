The constraint dual value in the best barrier iterate.

Only available for continuous LP and convex QP models, and only when the model was solved with the barrier solver (as
part of concurrent optimization or when barrier was invoked alone).